<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Cafe Thủy Mộc</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="bg-gray-50">
    <header class="bg-white shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-2">
                    <img src="~/images/logo.jpg" alt="Cafe Thủy Mộc Logo" class="h-12 w-12 rounded-full object-cover" />
                    <div>
                        <a asp-controller="Home" asp-action="Index" class="text-xl font-bold text-gray-900 no-underline">Cafe Thủy Mộc</a>
                        <p class="text-xs text-gray-500">Cà phê thơm ngon - Không gian xanh</p>
                    </div>
                </div>
                
                <nav class="hidden md:flex space-x-8">
                    <a asp-controller="Home" asp-action="Index" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:text-amber-600 no-underline">Trang chủ</a>
                    <a asp-controller="Menu" asp-action="Index" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:text-amber-600 no-underline">Thực đơn</a>
                    <a asp-controller="Promotion" asp-action="Index" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:text-amber-600 no-underline">🎁 Khuyến mãi</a>
                    <a asp-controller="Admin" asp-action="Dashboard" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:text-amber-600 no-underline">Admin</a>
                </nav>
                
                <div class="flex items-center space-x-4">
                    @if (Context.Session.GetString("UserId") != null)
                    {
                        <span class="text-sm text-gray-600">Xin chào, @Context.Session.GetString("UserName")</span>
                        <a asp-controller="Auth" asp-action="Logout" class="text-sm text-gray-600 hover:text-amber-600 no-underline">Đăng xuất</a>
                    }
                    else
                    {
                        <a asp-controller="Auth" asp-action="Login" class="text-sm text-gray-600 hover:text-amber-600 no-underline">Đăng nhập</a>
                        <a asp-controller="Auth" asp-action="Register" class="bg-amber-600 text-white px-4 py-2 rounded-lg text-sm hover:bg-amber-700 no-underline">Đăng ký</a>
                    }
                    
                    <a asp-controller="Cart" asp-action="Index" class="relative p-2 text-gray-600 hover:text-amber-600 no-underline">
                        <i data-lucide="shopping-cart" class="h-6 w-6"></i>
                        <span id="cart-count" class="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center" style="display: none;">0</span>
                    </a>
                </div>
            </div>
        </div>
    </header>
    
    <main>
        @RenderBody()
    </main>
    
    <footer class="bg-amber-900 text-white py-8 mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p>&copy; 2024 Cafe Thủy Mộc. Tất cả quyền được bảo lưu.</p>
        </div>
    </footer>
    
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script>
        lucide.createIcons();
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>